// const config = {
//   backendUrl: 'http://localhost:3060/api' //process.env.REACT_APP_BACKEND_URL,
// };

// console.log(`backendUrl in config.js: ${config.backendUrl}`)
// export { config as urlConfig }

export const urlConfig = {
  apiUrl: 'http://localhost:3060/api', // No trailing slash
};